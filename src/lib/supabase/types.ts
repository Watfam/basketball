/**
 * Placeholder for generated Supabase types.
 *
 * Once the schema in supabase/schema.sql is applied to your project,
 * generate real types with the Supabase CLI:
 *
 *   npx supabase gen types typescript --project-id <your-project-ref> \
 *     --schema hoops > src/lib/supabase/types.ts
 *
 * Until then, this loose type keeps the client wrappers compiling
 * without false type safety.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
